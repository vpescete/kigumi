[package]
name = "kigumi-schema"
description = "Projections from a resolved Kigumi model: Postgres DDL and agnostic UI contract (JSON)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
kigumi-core = { workspace = true }
serde_json = "1"
