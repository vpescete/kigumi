[package]
name = "meshble-core"
description = "Meshble metamodel: inspectable model descriptors, extension resolution, module manifests"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
semver = { workspace = true }
inventory = "0.3"
serde_json = "1"
rust_decimal = "1"
