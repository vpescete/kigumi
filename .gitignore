/target
# Sparse checkout di Odoo usato solo come riferimento di analisi — non parte del repo.
/odoo19

# Instance config & secrets (real files — keep only the .example)
/kigumi.toml
.env

# Local Claude session state
.claude/
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
